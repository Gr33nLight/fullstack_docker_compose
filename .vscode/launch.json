{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Debug with Nodemon",
            "request": "launch",
            "command": "npm run server",
            "cwd": "${workspaceFolder}"
        },
    ]
}